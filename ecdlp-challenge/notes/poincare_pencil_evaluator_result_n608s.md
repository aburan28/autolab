# Result: N608S evaluates the selected H018 pencil on the open surface

For every nonzero `Q` in `E(F_103)`, inverse graph evaluation produces an
`F_103` coefficient vector for the selected cover section `x`. This defines
the open-surface pencil coordinate

```text
lambda(P,Q) = f_x,Q(P).
```

On all 108 nonzero rational cover points, it satisfies

```text
lambda(phi(R), pi(R)) = x(R).
```

On the declared affine domain of 11,448 pairs, the coordinate reaches all 103
base-field values; level-set counts range from 82 to 156. The 324 origin and
Cauchy-support chart exclusions are preserved rather than assigned values.

This is an evaluator receipt, not a relation mechanism. Smooth projective
pencil geometry, factor-base source return, relation rank, descent, cost, and
ECDLP relevance remain open.
