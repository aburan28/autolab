# Result: N608P constructs a target-induced chart away from the origin

For each of the 108 nonzero points in the N606Y `[84]` orbit, the N608J graph
matrix `V_Q` is invertible. Inverting the deck-fibre values of the three
degree-three cover sections `1`, `x`, and `y` yields coefficient vectors fixed
by the `F_103` Frobenius in every fibre.

Using a fixed rational lift of the base step gives 107 adjacent transitions on
the punctured orbit. Every transition is invertible and base-field valued, and
all fix the constant source coefficient. The independent verifier recomputes
all matrices, inversions, and transitions.

This is an explicit target-induced chart on the punctured base. It has not
crossed the origin, so it does not choose the origin quotient direction or the
two-dimensional global H018 section space. No relation, descent, cost, or
ECDLP claim follows.
