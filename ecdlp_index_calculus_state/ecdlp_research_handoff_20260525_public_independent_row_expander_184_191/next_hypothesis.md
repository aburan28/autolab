# Next Hypothesis

The next selector should change the source of leaf evidence, not only increase
row diversity in the existing low-term-total2 candidate pool.

Recommended 192-199 or live-state test:

1. Keep the public single-hit-root FFE gate and replay harness unchanged.
2. Generate or mine candidate cases from an alternate public leaf stream:
   total3/total4 low-term selectors, wide public leaf policy, hit-stream
   FFE candidates, or a public leaf-break contrast source.
3. Require the source to expose target, transfer index, row key, leaf indices,
   surface ID, and public ops/rho so it can be bridged into the existing replay
   format.
4. Preregister selection by public root-cost plus leaf-source diversity only.
5. Replay same-challenge groups and compare against the 184-191 expander
   baseline.

Success criterion:

Promote only if the alternate leaf/hit stream raises `67.a1@9803` to rank 2
with public-key verification below the Pollard-rho proxy on a fresh or
predeclared window.

Failure criterion:

If alternate leaf streams still produce only zero-event or duplicate rank-1
forms, move away from low-term support selection and test a structural FFE
feature that predicts independent relation forms directly.
