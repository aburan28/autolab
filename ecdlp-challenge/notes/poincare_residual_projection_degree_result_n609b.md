# Result: N609B certifies degree-nine residual P-fibres on the punctured base

## Status

`RESTRICTED THEOREM / DEGREE-NINE RESIDUAL P-FIBRE ON DECLARED PUNCTURED BASE
/ STRUCTURAL REPLAY / MODEL-BOUND / TOY-EVIDENCE / GLOBAL CLASS OPEN /
NO_ECDLP_CLAIM`

## Result

In the denominator-cleared equation, `c_7 x^5` is the unique order-ten term at
`P=O`: `ord_O(x)=-2`, `ord_O(y)=-3`, and the competing `B_Q(x)y` terms have
pole order at most nine. The `c_7` coefficient is nonzero for every one of the
108 nonzero rational base points. Removing that leading term lowers the pole
order to nine, which is the control for the calculation.

For each declared level `0`, `1`, and `17`, every fibre therefore has:

```text
pole degree at P=O:       10
common graph zero P=4Q:    1
residual divisor degree:   9
```

Thus the N609A degree-nine residual root recovery has not omitted an additional
zero at `P=O` on the declared punctured base.

## Interpretation

The residual inverse now has a divisor-degree justification in its `P`
fibres, rather than only a polynomial-degree observation. This still leaves
the `Q=O` boundary, second projection, compact global class, normalization,
and any relation mechanism unresolved.

## Next Action

Determine the `Q=O` boundary and second projection degree of the compact
residual curve. Only a globally identified divisor can be used to test genus,
Jacobian structure, factor bases, relations, rank, descent, and cost.
