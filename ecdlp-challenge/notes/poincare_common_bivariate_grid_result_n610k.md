# N610K Result: common finite H018 bivariate chart grid

For each of levels `0`, `1`, and `17`, all 144 coefficients in the `12 x 12`
`v,u` grid agree between the two exact-Cauchy Laurent orders.  The grids contain
`71`, `70`, and `70` nonzero coefficients respectively.

This is a common finite local chart datum, not a proof of an all-orders
identity or a global normalization.  Its next use is an independent precision
sweep and a literal transition derivation before any relation or ECDLP claim.
