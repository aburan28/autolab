# Next Hypothesis

Keep the same frozen guarded public-factor portfolio and materialize the next
available transfer window after 280-295.

Promotion evidence for the next window:

- Public compact-leaf stress produces nonempty fixed-policy below-rho cases.
- Sage materializes preserving nontrivial resultant factors for every emitted
  FFE surface.
- Direct quadratic-root recovery is false-positive-free.
- The frozen guarded portfolio recovers, preserves, and stays below rho on
  every selected surface.
- The LOO audit with the new window still has zero oracle gap and zero failing
  held-out windows.

Demotion evidence:

- Any guard-passed false positive.
- Any new held-out surface where the guarded oracle is below rho but the public
  portfolio is not.
- A repeated need for hash-only rescues with no measurable structural factor
  metadata.
- Full-remainder costs staying above rho while factor-surface wins fail to
  transfer to larger or more varied public row families.

Concrete next commands:

1. Run the public compact-leaf stress probe for the next transfer block with
   absolute `/Volumes/Volume/autolab/...` input sources and local output.
2. Summarize with
   `frontier_signed_eval_cover_row_side_sketch_fresh_salt_filter_rescue_guarded_relation_harvester_static_bank_shared_challenge_salt_neighborhood_low_term_total2_expanded_leaf_rescue_summary_probe.py`,
   filtering to `fixed_target_cap2_ow0_hw3_lw0_sw0_cw0_aw1`.
3. Run the Sage factor probe with `/usr/local/bin/sage --python`.
4. Run the public-factor quadratic-root probe with
   `/Users/adamburan/.cache/vllm-venvs/vllm-312/bin/python`.
5. Append the new quadratic-root artifact to the guarded rolling and LOO
   window lists with `min_surfaces=2` and hash policies included.

Parallel structural audit:

Explain why the hash policies are useful.  Compare factor metadata for
hash-selected rows versus structural policies on the 67.a1@9803 surfaces:
factor degree, coefficient support, selected zero leaf index, target, transfer
index, and charged cost.  If the hash order correlates with a public low-degree
shape, replace it with an explicit structural order; otherwise keep it as an
empirical public selector and avoid overclaiming mechanism.
