# Hypothesis

The guarded public-factor portfolio should promote from retrospective
robustness to a fresh-window result if the public compact-leaf source, Sage
resultant factors, direct monic quadratic root recovery, and guarded portfolio
selection all remain below rho on transfer indices 280-295 without changing
the portfolio eligibility rule.

Frozen portfolio settings:

```text
guard: selected_leaf_count_eq1_and_factor_zero_eq1
policy eligibility: include hash policies
min_surfaces: 2
min_preserve_ratio: 1.0
min_recovered_ratio: 1.0
max_false_positive_rate: 0.0
max_train_worst_ratio: unset
held-out selection: cheapest_guarded_public_zero
```

The mechanism claim is intentionally narrow: this is evidence for a public
FFE/resultant quotient component with direct quadratic root recovery, not a
complete asymptotic ECDLP index-calculus algorithm.
