# N609S Result: naive H018 product crossing chart is obstructed

The determinant-weighted raw coefficient valuations are identically
`[-8,-6,-5,-4,-3,-2,-1,0,-7]` at levels `0,1,17`. These Q-poles align with
the P-pole weights, so the ordinary product chart cannot be used as a regular
crossing extension. The concrete positive successor is a weighted blow-up
using slope `v/u`; this is a scoped chart obstruction, not a global
impossibility or an ECDLP result.
