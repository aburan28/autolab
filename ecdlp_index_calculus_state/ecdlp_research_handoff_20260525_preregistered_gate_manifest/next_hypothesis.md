# Next Hypothesis

A fresh 80+ surface bank that passes `pre_factor_nonvacuous_hit_root_proxy`
will retain the root-hyperplane below-rho signal under a frozen public root
policy.

Concrete next work orders:

1. Generate or collect fresh 88-95 total2/total3/total4 candidate cases without
   using post-factor root-policy outcomes.
2. Run `ffe_preregistered_gate_manifest_probe.py` first, with selector joining
   disabled or ignored, and freeze the selected surface manifest.
3. Factor only the preregistered surfaces with Sage.
4. Audit first-fall shape, then run the frozen root-hyperplane selector.
5. Report materialized count, pre-factor gate count, public-zero-capable count,
   preserving count, conservative scan wins, and direct-root wins separately.

Success for the next step is not "any below-rho row."  Success is a fresh
pre-factor-gated manifest whose post-factor selector evaluation remains below
rho without adding an ex post public-zero filter.
