# P1553 5A5C Shared Semilinear Incidence Gate R93

## Claim boundary

No generic-prime-field ECDLP algorithm, Pollard-rho improvement, Shoup-bound
improvement, factor-log solve, or target descent is claimed.

Classification:

```text
CONSTANT_S3_RESULTANT_OPERATOR_EXACT__ZERO_INCIDENCE_FULL_RANK_AT_78_CHARTS__ROOT_SOURCE_FEATURE_ROWS_B12O5__PROJECTIVE_SOURCE_UNRANKING_ABSENT
```

## Shared operator

For

```text
f(z)=a*z^2+b*z+c,
g(z)=d*z^2+e*z+f,
```

their resultant is bihomogeneous of degree two in each coefficient triple.
With

```text
Ver2(a,b,c)=(a^2,ab,ac,b^2,bc,c^2),
```

R93 freezes an explicit `6x6` matrix `K` satisfying

```text
Res(f,g)=Ver2(a,b,c)^T K Ver2(d,e,f).
```

The matrix has exact rank six over `F_101`. The identity is verified on all
`78^2` pairs of `S3` quadratics generated by twelve frozen x-coordinate
classes on

```text
E/F_101: y^2=x^3+7x+4,
#E(F_101)=97.
```

Thus the proposed shared semilinear operator is real and constant-size.

## Value rank versus zero rank

For deck sizes `4,6,8,10,12`, the unordered pair-chart counts are

```text
10, 21, 36, 55, 78.
```

At every scale, both the Veronese feature matrix and raw resultant-value
matrix have rank six. Applying the exact zero predicate gives incidence
ranks

```text
7, 18, 29, 53, 78.
```

The largest zero-incidence matrix is full rank even though its raw value
kernel has rank six. The nonlinear zero projection therefore does not inherit
the semilinear rank bound.

Across all five scales, `S4=0` is equivalent to existence of a signed
four-point relation. There are zero predicate/source mismatches; every zero
returns one exact signed affine source. Both proper-subsum and full-only
branches occur. Five deterministic random-quadratic controls preserve the
same low raw rank versus higher zero-incidence-rank separation.

This is finite exact evidence, not an asymptotic rank-growth theorem.

## Full-source cost

At the R84 root split,

```text
(2A+3C) versus (3A+2C),
```

the source-bearing side counts have exponents

```text
B^(12/5) and B^(13/5).
```

A fixed-width feature vector per source does not change those row counts.
The smaller explicit feature body already exceeds the `B^(9/4)` setup cap,
and scanning either side exceeds the `B^(5/4)` fresh-work cap.

The constant operator compresses one comparison. It does not construct,
index, or unrank the source rows.

## Projective boundary

The finite `S3` replay uses affine x-coordinate charts. Infinity is not
represented, and nonreduced tangent multiplicity is not source-complete.
Actual compact 5A+5C unranking, known-RHS rank, factor logs, and identical
descent remain absent.

Nine of twenty obligations pass.

## Scope

This rejects materializing one fixed-width feature row per R84 root source
and treating a low-rank resultant-value kernel as a low-rank zero projector.
It is not a lower bound on an implicit Cartesian range index, a nonlinear
source-returning circuit, or another FFE representation.

Disposition:

```text
REJECT_MATERIALIZED_SHARED_RESULTANT_FEATURE_ROWS_ONLY__RANK_SIX_VERONESE_OPERATOR_EXACT__AFFINE_S4_SIGNED_SOURCE_BICONDITIONAL_EXACT__ZERO_INCIDENCE_FULL_RANK_AT_78_CHARTS__ROOT_FEATURE_BODIES_B12O5_AND_B13O5__INFINITY_AND_FULL_5A5C_UNRANKING_ABSENT__IMPLICIT_CARTESIAN_RANGE_INDEX_OPEN__NO_RANK__NO_FACTOR_LOGS__NO_DESCENT__NO_SHOUP_CLAIM__NO_BREAKTHROUGH
```

## Exactly one next action

Construct or refute one implicit Cartesian Veronese hyperplane source index
for the R84 root split. It must answer the exact bilinear zero query and
return coupled 5A+5C source indices without emitting `B^(12/5)` feature rows,
fit `B^(9/4)` setup and `B^(5/4)` fresh work/workspace, and replay infinity,
proper-subsum, tangent, multiplicity, and blind-zero branches without DLP
labels or verifier oracles.
