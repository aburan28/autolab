# N610E Result: bounded coefficient-wise H018 formal branch

Every coefficient through `u^24` replays on held-out slopes at its own recorded
degree.  Within that bounded jet, the level-zero branch through slope `83` has
correction valuation four and residual valuation `46` in the truncated model.
The corrected level-one and level-seventeen differences have exact order-eight
unit leading terms `42` and `96`.

This supersedes the inadmissible branch numbers from N610C's failed fixed
degree-nine reconstruction.  It is still a bounded, replayed formal-jet
observation, not a proof of an exact two-variable chart or local colength, and
it has no global geometric or ECDLP consequence.
