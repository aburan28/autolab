# P1436 autoresearch focus harness v16 result

## Status

`DIAGNOSTIC_ONLY_WITHHOLD_PROMOTION`

No generic-prime ECDLP algorithmic breakthrough or Shoup-bound improvement was
found. This continuation closes two repeatedly suggested structured/FFE lanes
against the current live receipts and makes the remaining producer gap the
single first action.

## IDEA-340 public-chart preflight

P1407, P1408, P1416, P1432, and R68 are bound by SHA-256 and evaluated as one
seven-obligation attack preflight.

- public DLP-free construction: pass;
- exact rank and target descent: pass (`99/99` policy-curves, `792/792` targets);
- held-out additive-energy promotion: fail for every public policy;
- prospective full-rank persistence: fail (`1/78` cells);
- source exponent: fail (`n^0.6` explicit join, `n^0.6750005447` selected work);
- held-out end-to-end cost: fail (`21.99755781x` to `65.93653101x` rho);
- fresh persistence: fail.

The lane passes `2/7` obligations and is rejected. The generator-multiple
positive control receives no attack credit.

## Slice-quadratic source preflight

The current slice-quadratic probe remains a valid post-selector component:
`17/18` surfaces preserve the selected roots below rho after direct quadratic
root solving.

It is not a public relation source. The bound producer obtains each slice from
`selected_leaf_indices`; the corpus has only two target families, 19 positive
cases, no negative cases, no frozen target-level split, no complete rejected
candidate costing, and no fresh independent fixed-sum row delta.

Even granting one free operation per axis/value candidate, a label-blind
uniform schedule needs an expected `52.2853x` to `57.0876x` rho before reaching
one of the two oracle slice values. Actual specialization, factorization,
rejection, and leaf scanning can only increase this cost. The source lane
therefore passes `0/6` obligations.

## Harness routing

Harness schema v8 binds both preflights and treats their non-admission as live
frontier state:

- `idea340_public_chart`: closed by current evidence;
- `slice_quadratic_public_source`: oracle diagnostic only;
- first focus item: `public_slice_source_corpus`;
- promotion allowed: false.

Product/summation quotient relations continue to receive zero independent-rank
credit under R68; only fresh fixed-sum rows may reopen the FFE lane.

## Verification

- ECDLP task tests: `65/65` passed;
- Python compilation: passed;
- `git diff --check`: passed;
- IDEA-340 preflight SHA-256:
  `68586635779f84ad7b754a36d54c67a7b40f5967169ac8f43e26aac5db7b541a`;
- slice-source preflight SHA-256:
  `b21f0aa1792487566395b81e881d865ee802d42145d7904c21e78b78d03bd716`;
- v8 focus report SHA-256:
  `5d6321f623e4589c0923d433e620187cfafa48f4580d967eb12c4fcc393e7d45`;
- exact multiscale collision receipt SHA-256:
  `ecccc5bfdf4c78a23afdd98938ef867a386b5c9c246d27a4d630e6fea076a754`;
- R68 receipt SHA-256:
  `5c064a36630c052093ebb7bd3b4429d37f836982ba097eacbad2f2ebe1bb6b51`.

Exactly one next action: modify the surface producer to emit a label-separated
corpus over at least four target families with deterministic public pre-choice
axis/value/factor candidates, sequestered positive and negative outcomes,
every rejected-candidate cost, and fresh fixed-sum row/rank deltas; then run
leave-one-target-family-out exactly once.
