# N610I Result: naive H018 reverse-order clearance rejected

The `v-u` factor that regularizes N610H's ordered chart does not give a
symmetric bivariate section.  At every tested level, the reverse expansion has
v-valuations `1, 0, -1, -2, ... , -14` on the `u^0` through `u^15`
coefficients.

This is a reusable local obstruction: a further transition correction is
needed before any global Cartier or normalization path can proceed.  It does
not rule out a corrected chart or another representation.
