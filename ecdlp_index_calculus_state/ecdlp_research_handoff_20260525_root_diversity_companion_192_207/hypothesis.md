# Hypothesis

Public FFE root-policy anchors are not enough by themselves: retaining the full
public row/leaf companion assembly around root-diverse anchor cases may recover
the independent relation forms needed for rank-2 derivation on fresh transfer
windows.

Selection must remain public-only.  The selector may use target, transfer,
row salt, row key, row schedule key, selected leaf signature, top-k,
leaf selector, root-hyperplane root, and public ops/rho.  It must not select on
public-key verification, relation count, rank, preserving labels, false-positive
labels, or derived secrets.
