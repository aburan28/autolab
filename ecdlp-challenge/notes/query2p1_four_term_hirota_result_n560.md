# N560 Result: four-term quadratic Fay/Hirota screen

No all-term centered four-term identity survives the independent-deck matrix
on the registered `F_251`, `F_257`, and `F_269` rows, even when each
coefficient is post-fit in the affine monomial basis of `L(12O)`.

Every exact raw source-root support replays and every shuffled control rejects,
so the null result is not caused by a broken value channel. This rejects this
specific four-term, base-field affine-coefficient family only; it does not
rule out higher-term, non-affine, or source-labelled nonlinear operations.
