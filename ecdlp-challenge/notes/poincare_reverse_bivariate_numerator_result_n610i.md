# N610I Result: naive H018 reverse-order clearance rejected

The base-coordinate tangent proxy `v-t` that regularizes N610H's ordered chart
does not give a symmetric bivariate section.  At every tested level, the
reverse expansion has v-valuations `1, 0, -1, -2, ... , -14` on the `t^0`
through `t^15` coefficients.

This is a scoped obstruction for the tangent proxy only.  N610J supplies the
missing exact support-parameter correction `v-w(t)` and supersedes it for
literal local-transition work.
