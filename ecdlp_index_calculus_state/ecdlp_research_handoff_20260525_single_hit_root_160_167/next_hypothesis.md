# Next Hypothesis

Two next steps should run in parallel.

First, preregister a third disjoint total2 replication on transfer indices
168-175:

- Channel: total2 only.
- Gate: `single_prefactor_hit_root`.
- Primary policy: `prefactor_unique_leaf_hit_root_first_target_hash`.
- Promotion target: all retained surfaces preserving, false-positive-free, and
  scan-below-rho.
- Controls: broad all-prefactor policies plus an explicit multi-hit audit.

Second, start converting the component into a relation-assembly bridge:

- Use the 152-167 retained root hyperplanes as concrete relation-generation
  events.
- Group by target and challenge seed.
- Count independent verifier equations obtainable from only retained
  first-fall hyperplanes.
- Charge the online path against the same `generic_rho_steps`.
- Report honestly whether the bridge derives an ECDLP secret or remains only a
  component-level route.

Open obligations:

- No end-to-end ECDLP speedup is claimed yet.
- Multi-hit-root surfaces need their own discriminator before inclusion.
- Total3/4 stays excluded until a public channel discriminator rejects the
  known non-preserving total3/4 failure mode.
