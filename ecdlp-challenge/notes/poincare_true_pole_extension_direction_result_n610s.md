# N610S Result: extension-field true-pole direction screen

For the first sixteen nonexceptional powers of the generator of `F_(103^6)`
and each selected level, all 48 weighted transition rows pass: the weighted
true-pole representative is regular, the transition factor is a unit, and the
crossing identity is exact.

This rules out a base-field-only artifact in this deterministic screen. It
does not cover every geometric direction, the true-pole center or
inverse-support/P-origin charts, global Cartier geometry, normalization,
relations, or any ECDLP cost claim.
