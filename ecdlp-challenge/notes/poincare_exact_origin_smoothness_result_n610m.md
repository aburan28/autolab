# N610M Result: no H018 singularity candidate at the exact simultaneous origin

At levels `0`, `1`, and `17`, the exact-Cauchy numerator has zero constant
coefficient and the same nonzero linear gradient `(86,86)` in the local
variables `v,u`.  Each selected member is therefore nonsingular at this chart
point.

This closes one boundary singularity candidate only.  It does not establish a
global Cartier divisor, global smoothness, normalization, Jacobian, relation
law, or ECDLP advantage.
