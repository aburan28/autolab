# N610U Result: generic P-origin-to-crossing transition screen

On the simultaneous generic direction `p=c*u` over `F_(103^6)(c)`, the raw
evaluator has the expected order-eight P-origin pole. Multiplication by `p^8`
produces a regular representative, and the crossing representative is exactly
`(p-u)` times that P-origin frame through precision 32 at all selected levels.

This establishes a generic overlap convention only. The exact P-origin
direction `c=0`, all-orders transition data, global Cartier geometry,
normalization, relations, and ECDLP cost claims remain open.
