# Hypothesis

The 184-191 total2 expander showed that `67.a1@9803` does not become rank 2
by adding more same-family total2 rows.  The next hypothesis is that the hard
target needs a wider public leaf family:

1. Freeze the previously validated public row selectors.
2. On fresh transfers 192-199, scan only total3 and total4 low-term-support
   leaf selectors.
3. Aggregate strict below-rho verifier-backed signatures.
4. Gate the resulting FFE surfaces with the preregistered pre-factor
   selected-hit-root proxy.
5. Sage-factor only selected surfaces and score roots with the same
   prefactor unique-leaf single-hit-root policy.
6. Replay retained-root assemblies through the verifier.

Selection forbids public-key verification, relation count, rank, preserving
labels, and false-positive labels in the FFE/root-policy steps.
