# Hypothesis

Freeze the 200-207 fourth-leaf mechanism and test it on a fresh total3/total4
window.

The prior below-rho recoveries on 200-207 were driven by exact marginal
total4-over-total3 lifts, strongest at `22050.cf1@11731` with base leaf `[90]`
and added leaf `[34]`.  A real candidate algorithm needs this rule to survive a
fresh window without choosing leaves from verifier labels.

This run tests transfer indices 208-215 with the same public fixed-row
selectors, total3/total4 low-term leaf selectors, pre-factor FFE gate, Sage
factorization, and prefactor unique-leaf root policy used in the successful
200-207 route.
