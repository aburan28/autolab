# P1436 autoresearch focus harness v3 result

## Status

`HARNESS_ENRICHMENT_PASS / NO_P1436_CAMPAIGN_RESULT / NONPROMOTING`

This version binds the process guidance from
`https://x.com/askalphaxiv/status/2076737985559822734` and the self-patching
method in arXiv:2607.08393 to explicit P1436 diagnostics. The transfer remains
methodological: P1436 configuration replays are not causal activation patches.
`methodology.tweet_guidance` is now explicit in the output, recording the
bounded-critical-budget and deterministic non-blocking ambiguity policy, with
human-readable notes for each constraint in the generated focus note.

## Changes

- The report schema is `ecdlp.p1436_autoresearch_focus_report.v3`.
- Every cell now records fixed and oracle deltas at collision supply,
  cross-shift routing, row extraction, rank, verified logs, and charged cost.
- Target-policy rank headroom is aggregated by curve split. Prospective transfer
  distinguishes missing evidence, exactness failure, rank regression, positive
  transfer, and no-headroom/no-effect outcomes.
- Fixed-minus-natural rank gains for `two_map_union` are paired with every exact
  available hash-policy replay. A nonpositive excess records that a generic
  scheduler effect has not been ruled out.
- The paper's 58--75% LLM headroom result is not imported as an ECDLP threshold.
- The natural collector, factor-log, target-descent, cost, source-claim, and
  independent-audit promotion gates are unchanged.

## Verification

`python3 -m unittest -v tasks/ecdlp_index_calculus/tests/test_p1436_autoresearch_focus_harness.py`
passes `10/10` tests. Python compilation and `git diff --check` pass for the
harness, tests, and contract.

The tests cover positive and falsified matched-hash specificity, prospective
headroom aggregation, route-delta attribution, deterministic occupancy, bounded
focus, missing descent, audit hash binding, CLI output, and source-post binding.

## Bound Artifacts

- harness SHA-256: `6fbcd1c08e47c05bcada86d94cdf2b63ba96cd8645e51d6bb2cefa0b055783b7`
- tests SHA-256: `fe5846f6d11fc47fe8196e756b8ad24b5c585839cd612822d40806497faead7e`
- contract SHA-256: `577de895489ea153d3f64543abf2c1f7ca2e771899802a8f8f0a43f1d73364f7`
- report artifact SHA-256: `9a62d4e80f07ed368fc04daef64e4920e54ee6c14f4fe177e33e085508dd5e30`

## Campaign Boundary

The default P1436 producer artifact
`p1436_large_prime_residual_collision_collector_after_p1435_probe.json` remains
absent in this worktree and `/Volumes/Volume/autolab`. The mounted producer was
inspected for schema compatibility; only `hash_control_0` receives the full
configuration replay, so unavailable hash interventions are excluded rather
than counted as failures.

No collector or contract was executed. This is harness evidence only and does
not support or reject P1436 arithmetic, rank, descent, cost, or a better-than-rho
ECDLP claim.
