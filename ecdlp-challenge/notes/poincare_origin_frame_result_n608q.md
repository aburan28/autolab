# Result: N608Q gives the formal source frame at the origin

With `u=-x(r)/y(r)`, the moving Cauchy kernel has the exact Laurent expansion
whose coefficients through `u^7` lie in the fixed eight-dimensional space
`L(8O)`. The `u^8` coefficient has nonzero `x^3y` component, completing the
standard `L(9O)` basis.

Therefore

```text
u^(-8) (k_r - sum_(e=-1)^7 u^e c_e)
```

has a finite, new limiting direction at the origin. Independent interpolation
and six held-out point checks reproduce every coefficient.

This resolves the source-frame degeneration only. The target `O(3Oprime)`
local frame, the rank-eight origin evaluation, the quotient functional, global
two-section subspace, source recovery, relation rank, descent, cost, and ECDLP
relevance remain open.
