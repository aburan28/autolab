# Next Hypothesis

Turn the route ensemble into a pre-registered future-surface selector.

Concrete next steps:

1. Generate or identify the next FFE quotient transfer window beyond the 18
   current surfaces.
2. Freeze the route choice:
   - use direct public factor ranking when its public all-hit estimate is below
     rho;
   - otherwise use the calibrated fingerprint leaf-locator when its public
     all-hit estimate is below rho;
   - choose the lower public estimated ops/rho when both are available.
3. Keep labels out of selection. Only audit `preserves_selected_root_pairs`,
   `same_selected_root_pairs`, and verifier-backed relation recovery after the
   public route is chosen.
4. Attack the single known miss by looking for a cheaper public factor route on
   `22050.cf1@11731` transfer 57, salt 164. The current preserving fingerprint
   route is near rho at `ops/rho = 1.06569343`, so a small cost reduction could
   close the 18/18 surface set.
5. If the future-surface test passes, integrate the ensemble into the
   low-term total-2 FFE quotient generator as the public route-selection layer.

Promotion rule:

Do not call this an index-calculus breakthrough until the pre-registered route
ensemble produces below-rho verifier-backed relations on fresh generated
surfaces while keeping row materialization, factorization, and root recovery in
the cost model.
