# N610R Result: full nonexceptional base-field true-pole direction sweep

For every base-field direction `r=2..101` and each selected level, all 300
weighted transition rows pass: `u^7 zD` is regular, the transition factor is a
unit, and the crossing identity is exact.  N610Q supplies the separate tangent
direction `r=1`.

The origin and inverse-support directions require their own boundary charts,
and this sweep does not cover extension-field directions or establish global
Cartier geometry.
