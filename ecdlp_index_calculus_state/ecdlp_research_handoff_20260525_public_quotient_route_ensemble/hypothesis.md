# Hypothesis

The FFE quotient branch is blocked less by root recovery itself than by public
route selection. Two public selectors appear complementary:

- direct public factor ranking finds cheap quotient factors on the
  `67.a1@9803` surfaces;
- calibrated factor-fingerprint plus leaf-location support finds cheap
  quotient factors on the `22050.cf1@11731` surfaces.

If the route choice uses only public estimated cost, the union may cover more
relation surfaces below Pollard rho than either selector alone, without adding
verifier-label leakage.
