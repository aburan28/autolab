# Hypothesis

Declare `learned_target_zero_residue16_prior` before the 104-111 window and
score it only after the usual strict signature, pre-factor gate, Sage
factorization, and first-fall root-hyperplane audit.

The policy identity is selected from the explicit 96-103 report, where it
preserved both gate-selected surfaces with no false positives and below-rho
scan/direct accounting while its root-ordering statistics were trained only on
the <=87 gated surface rows.

Promotion condition:

- The pre-factor gate must use only materialized-surface nonvacuity and
  selected-leaf hit-root proxy fields.
- Sage factorization must be run only on pre-factor gate-selected surfaces.
- The chosen root policy must preserve all original selected root pairs.
- The chosen root policy must have zero false positives.
- The chosen root policy must be below Pollard rho under scan or direct-root
  accounting on every gate-selected surface.

Baselines declared before the run:

- `learned_global_zero_root_prior`
- `low_root_norm`
